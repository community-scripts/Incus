# Incus-specific metadata

Empty by design. Application metadata lives with the application scripts in
`community-scripts/ProxmoxVE/json/`, because the scripts themselves are shared.

This folder only takes metadata for entries that exist **exclusively** on Incus
— today, none do.
